#!/bin/bash

command=hdfs

# for some reason, .bash_profile isn't sourced with
# the -l argument. Source it explicitly.
sudo -u <%= node[:hdfs][:user] %> bash -l <<EOF
source /home/hdfs/.bash_profile
/srv/hadoop-2.2.0/bin/$command $@
EOF

exit $?


