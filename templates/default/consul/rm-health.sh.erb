#!/usr/bin/env bash

CHECK="curl --fail --silent http://$HOSTNAME:8088/ws/v1/cluster > /dev/null 2>&1"

source <%= node['consul']['health-check']['retryable-check-file'] %>

_run_health_check "$CHECK"