#!/usr/bin/env bash

CHECK="curl --fail --silent -k https://$HOSTNAME:8089/ws/v1/cluster > /dev/null 2>&1"

source <%= node['consul']['health-check']['retryable-check-file'] %>

_run_health_check "$CHECK"
