#!/usr/bin/env bash

CHECK="curl --fail --silent --insecure --key <%= @key %> --cert <%= @certificate %> <%= @scheme %>://$HOSTNAME:<%= @http_port %> > /dev/null 2>&1"

source <%= node['consul']['health-check']['retryable-check-file'] %>

_run_health_check "$CHECK"