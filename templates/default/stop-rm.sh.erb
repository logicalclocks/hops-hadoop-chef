#!/bin/bash

command=resourcemanager
pidDir=<%= node[:hadoop][:logs_dir] %>
service=yarn

<%= node[:hadoop][:home] %>/sbin/kill-process.sh $service <%= node[:hadoop][:yarn][:user] %> $command $pidDir

exit $?
